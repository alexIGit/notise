если пишет что установлена не полследняя версия npm

устанавливаем последнюю версию

sudo npm install -g npm

если: /usr/bin/env: «node»: Нет такого файла или каталога
то делаем символьную ссылку: 
 sudo ln -s /usr/bin/nodejs /usr/bin/node


